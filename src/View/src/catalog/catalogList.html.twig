{% extends 'src/layout.html.twig' %}

{% block content %}
    <div class="page cat_page">
        <div class="container">
            {% include 'src/common/breadcrumbs.html.twig' %}
        </div>
        <div class="container">
            <h1>Каталог</h1>
            <div class="row cat_row">
                {% for category in footer.categories %}
                    <div class="col-md-4 mb_30 wow fadeInUp" data-wow-delay="0.5s">
                        <a href="{{ category.url() }}" class="cat_item">
                            <img src="{{ category.image }}" alt="">
                            <div class="cat_name">
                                <h4>{{ category.name }}</h4>
                            </div>
                        </a>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}